import CanalesClient from './CanalesClient';

export const dynamic = 'force-dynamic';
export const metadata = { title: 'Canales OTA · Admin' };

export default function CanalesPage() {
  return <CanalesClient />;
}
